# AI-Vent-Share 资源手动更新指南

## 📋 适用场景

- 低频率资源更新（每周≤5条）
- 单人维护项目
- 无自动化需求
- 兼容阶跃AI轻量化解析

## 🔧 前置依赖

1. 本地安装 Python 3.6+
2. 项目核心文件：
   - `content_index.json` - 主索引文件
   - `docs/` - 资源文件存储目录
   - `static/indexes/` - 分片索引目录

## 📝 操作流程

### 方式1：极简手动编辑（推荐，单条资源2分钟完成）

#### 步骤1：创建资源文件
在 `docs/` 目录新建 `.txt` 文件，写入资源详情

```bash
# 示例：创建一个AI工具资源
notepad docs/ai_tool_example.txt
```

#### 步骤2：更新主索引
打开 `content_index.json`，在 `index` 数组末尾追加标准格式条目

```json
{
  "content_id": "ai_item_0159",
  "uuid": "生成新的UUID",
  "title": "资源标题",
  "content_type": "选择类型",
  "summary": "资源摘要",
  "keywords": "关键词",
  "compliance_status": "通过",
  "direct_link": "https://bzhanupsangejin.github.io/ai-vent-share/docs/文件名.txt",
  "full_content_anchor": "ai_item_0159",
  "trace_id": "trace_xxxxxxxx",
  "version": "1.0.0",
  "compliance_hash": "manual_verified"
}
```

#### 步骤3：生成分片索引
```bash
python scripts/generate_shard_index.py
```

#### 步骤4：提交代码
```bash
git add .
git commit -m "manual: 新增AI资源"
git push origin main
```

---

### 方式2：辅助脚本录入（批量添加推荐）

#### 步骤1：运行脚本
```bash
python scripts/manual_sync.py
```

#### 步骤2：按提示填写
```
=== AI资源手动录入工具 ===
1. 资源标题：[AI分享] 开源AI代码生成工具
2. 资源类型['AI工具', '代码模板', ...]：AI工具
3. 资源摘要（≤150字）：支持Python脚本自动补全
4. 完整内容（输入完成后按Enter，再输入END并按Enter结束）：
工具名称：CodeLlama
官方链接：https://github.com/...
END
```

#### 步骤3：自动生成
脚本会自动：
- 生成资源文件到 `docs/` 目录
- 更新 `content_index.json` 索引
- 提示执行分片索引更新

#### 步骤4：更新分片索引
```bash
python scripts/generate_shard_index.py
```

#### 步骤5：提交代码
```bash
git add .
git commit -m "manual: 新增AI资源"
git push origin main
```

---

## 📐 格式规范

### 1. 资源链接规范
- ✅ 正确：`https://bzhanupsangejin.github.io/ai-vent-share/docs/xxx.txt`
- ❌ 错误：`./docs/xxx.txt` 或 `docs/xxx.txt`

### 2. 文件大小限制
- 单文件≤10KB，适配阶跃AI算力
- 超过限制会影响AI解析性能

### 3. 内容类型约束
必须从以下8个标准类型选择，禁止自定义：
1. AI工具
2. 代码模板
3. FAQ文档
4. 免费API
5. 技术教程
6. 资源分享
7. 部署指南
8. 合规说明

### 4. 必填字段
- `content_id` - 唯一标识（格式：ai_item_XXXX）
- `uuid` - 全局唯一ID
- `title` - 资源标题
- `content_type` - 资源类型
- `summary` - 资源摘要
- `direct_link` - 资源链接

---

## 🔄 异常回滚

### 场景1：索引文件损坏
```bash
# 恢复备份文件
cp content_index.json.bak content_index.json
```

### 场景2：分片索引异常
```bash
# 重新生成分片索引
python scripts/generate_shard_index.py
```

### 场景3：Git提交错误
```bash
# 撤销最后一次提交（保留文件修改）
git reset --soft HEAD~1

# 或完全撤销（丢弃文件修改）
git reset --hard HEAD~1
```

---

## ⚡ 性能优化规则

### 必须执行的优化步骤

**每次新增/修改资源后，必须执行分片脚本**

```bash
python scripts/generate_shard_index.py
```

这样可以：
- 保证AI解析流畅
- 降低阶跃AI算力消耗
- 支持按类型定向拉取

### 分片索引的优势

- **O(1)查询** - 直接定位到具体分类
- **减少传输** - 只拉取需要的分类数据
- **降低解析** - 单个分片文件远小于完整索引

---

## 🧪 验证清单

### 添加资源后的验证步骤

- [ ] 资源文件已创建到 `docs/` 目录
- [ ] `content_index.json` 已更新
- [ ] 分片索引已重新生成
- [ ] JSON格式验证通过（无语法错误）
- [ ] 资源链接可正常访问
- [ ] Git提交成功
- [ ] GitHub Pages部署完成（1-3分钟）

### JSON格式验证

```bash
# 使用Python验证JSON格式
python -c "import json; json.load(open('content_index.json','utf-8')); print('✅ JSON格式正确')"
```

---

## 📊 当前项目状态

- **总资源数**：158条
- **代码模板**：66条
- **资源分享**：92条
- **其他类型**：0条

---

## 🔗 相关文档

- [资源添加标准化模板.md](./资源添加标准化模板.md) - 标准格式参考
- [README.md](./README.md) - 项目说明

---

## 📂 元数据文件规范路径

所有平台配置文件统一存放于：`/static/meta/`

1. **function_boundary.json** - 平台功能边界说明
2. **usage_guide.json** - AI+人类操作指引
3. **operation_meta.json** - 平台运营信息

AI可通过以下URL直接访问：
```
https://bzhanupsangejin.github.io/ai-vent-share/static/meta/function_boundary.json
https://bzhanupsangejin.github.io/ai-vent-share/static/meta/usage_guide.json
https://bzhanupsangejin.github.io/ai-vent-share/static/meta/operation_meta.json
```

---

## 💡 最佳实践

1. **定期备份** - 每次大量修改前备份 `content_index.json`
2. **小步提交** - 每次只添加1-2条资源，便于回滚
3. **验证后推送** - 本地验证无误后再推送到GitHub
4. **保持简洁** - 资源内容简洁明了，避免冗余信息
5. **统一格式** - 严格遵循标准模板格式

---

**最后更新**：2026年02月05日  
**维护者**：AI-Vent-Share项目组
